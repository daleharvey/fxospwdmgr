{
  "scripts": {
    "package": "zip -r ./package.xpi ./js manifest.json",
    "start": "nodemon --verbose --watch . -e js --exec npm run package"
  },
  "devDependencies": {
    "nodemon": "^1.4.1"
  }
}
